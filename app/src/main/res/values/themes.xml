<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Qapital" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Primary_Color</item>
        <item name="colorPrimaryDark">@color/Primary_mid</item>
        <item name="colorAccent">@color/primary_ice</item>
        <!-- Customize your theme here. -->
    </style>
</resources>