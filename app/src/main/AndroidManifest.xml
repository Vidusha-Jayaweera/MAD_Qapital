<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Qapital"
        tools:targetApi="31">

        <activity

            android:name=".activities.ExpenseFetchingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ExpenseDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ExpenseUpdateActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ExpenseInsertionActivity"
            android:exported="false"
            />




        <!-- SignInActivity -->
        <activity
            android:name=".activities.SignInActivity"
            android:exported="false" />

        <!-- signUpActivity -->
        <activity
            android:name=".activities.signUpActivity"
            android:exported="false" />

        <!-- HomeActivity -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false" />

        <!-- ProfileActivity -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />


        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>